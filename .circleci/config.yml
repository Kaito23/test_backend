version: 2.1
jobs:
    one:
        executor:
            name: node/default
            tag: '10.4'
        steps:
            - checkout
            - run: node --version
            - run: npm install
workflows:
    do_one:
        jobs:  
            - one
    

